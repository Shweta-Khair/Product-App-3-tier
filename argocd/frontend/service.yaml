apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: three-tier
spec:
  type: LoadBalancer  # changed from NodePort
  selector:
    app: frontend
  ports:
    - port: 80
      targetPort: 80
      # nodePort: 32000  # optional; typically omitted
